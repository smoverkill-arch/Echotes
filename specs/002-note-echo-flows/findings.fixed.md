# Findings Fixed Log

> Auto-generated by /speckit.fix-findings.run on 2026-05-12

## Summary

- **Total iterations**: 1
- **Findings resolved**: 4
- **Findings deferred**: 0
- **Final status**: CLEAN

## Iteration 1

### Findings Identified

- [Critical] `pendingReaderOpen` stayed alive after route mismatch, reload failure or missing note, contradicting the one-shot cleanup contract - `app/day/[date].tsx`
- [Warning] PowerShell review-run instructions used `--json` and the wrong script path, while the real detector accepts `-Json` under `.specify/extensions/review/scripts/powershell/` - `.agents/skills/speckit-review-run/SKILL.md`, `.specify/extensions/review/commands/run.md`
- [Warning] RPC constraint `23514` for continuation before the source note day was classified as `retryable_failure` instead of `invalid_input` - `src/features/notes/api/note-echo-errors.ts`
- [Info] Feature closure artifacts were stale: the spec still said `Rascunho` and Phase 6 evidence recorded the previous DocGuard check count - `specs/002-note-echo-flows/spec.md`, `specs/002-note-echo-flows/phase-6-evidence.md`

### Fixes Applied

- Fixed pending Reader cleanup so mismatched route day, reload error and missing expected note clear stale pending state before a future accidental open.
- Added integration coverage for clearing pending continuation opens when the expected note is absent after reload and when manual route navigation diverges from `noteDay`.
- Classified Postgres check-constraint error `23514` as `invalid_input`, matching the domain rule for `newNoteDay` before the source note day.
- Added unit coverage proving the continuation RPC `23514` path returns `invalid_input`.
- Corrected review-run PowerShell instructions to use the actual extension script path and `-Json`.
- Updated feature closure metadata from `Rascunho` to `Aprovada` and refreshed the recorded DocGuard consistency count.

### Findings Deferred

- None.
